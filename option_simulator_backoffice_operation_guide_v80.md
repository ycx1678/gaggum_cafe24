# 옵션 시뮬레이터 백오피스 운영 가이드

이 문서는 가꿈 상품상세의 `옵션 시뮬레이터`를 운영자가 관리하는 방법을 설명합니다.

앞으로 상품별 시뮬레이터 이미지는 Cafe24 스킨 파일이나 FTP에 직접 넣지 않고, 별도 백오피스에서 등록합니다. 스킨은 상품번호 기준으로 백오피스 API를 읽어 화면에 보여줍니다.

## 1. 운영 흐름

1. Cafe24 상품관리에서 상품 옵션을 먼저 등록합니다.
2. 백오피스에서 해당 상품을 선택합니다.
3. `시뮬레이터 사용`을 켭니다.
4. Cafe24 옵션명과 같은 이름으로 옵션군을 추가합니다.
5. 옵션값을 추가합니다.
6. 옵션 조합별 이미지를 업로드합니다.
7. 저장 후 상품상세에서 옵션을 선택해 이미지가 바뀌는지 확인합니다.

## 2. 옵션군 등록 기준

옵션군 이름은 사이트에 보이는 Cafe24 옵션명과 같게 입력합니다.

예시:

| Cafe24 옵션명 | 백오피스 옵션군명 |
| --- | --- |
| 상판 컬러 | 상판 컬러 |
| 프레임 컬러 | 프레임 컬러 |
| 의자 컬러 | 의자 컬러 |

내부 키값인 `top`, `seat`, `frame` 같은 개발용 이름은 운영자가 입력하지 않습니다.

## 3. 옵션값 등록 기준

옵션값도 사이트에 보이는 버튼명과 같게 입력합니다.

예시:

| Cafe24 옵션값 | 백오피스 옵션값 |
| --- | --- |
| 메이플 | 메이플 |
| 화이트 | 화이트 |
| 블랙 | 블랙 |

이미지에 영향을 주지 않는 옵션은 시뮬레이터에 등록하지 않아도 됩니다.

예시:

| 옵션 | 시뮬레이터 등록 |
| --- | --- |
| 상판 컬러 | 필요 |
| 프레임 컬러 | 필요 |
| 의자 컬러 | 필요 |
| 배송 방식 | 불필요 |
| 수량 | 불필요 |

## 4. 이미지 등록 기준

조합별 최종 이미지를 업로드합니다.

예시:

| 상판 컬러 | 프레임 컬러 | 의자 컬러 | 업로드 이미지 |
| --- | --- | --- | --- |
| 메이플 | 화이트 | 블랙 | 메이플+화이트+블랙 조합 이미지 |
| 월넛 | 블랙 | 베이지 | 월넛+블랙+베이지 조합 이미지 |

큰 원본 이미지를 올려도 상품상세에서는 시뮬레이터 영역 밖으로 커지지 않도록 자동으로 맞춰집니다.

## 5. 화면 노출 기준

- 상품상세에 처음 들어가면 기본 상품 썸네일이 보입니다.
- 옵션을 선택하거나 `옵션 시뮬레이터 보기`를 누르면 시뮬레이터 이미지가 보입니다.
- 백오피스에서 `시뮬레이터 사용`이 꺼져 있으면 버튼과 시뮬레이터가 보이지 않습니다.
- 해당 상품에 등록된 시뮬레이터 데이터가 없으면 상품상세는 기존 Cafe24 상세페이지처럼 동작합니다.

## 6. 백오피스 API 형식

스킨은 아래 주소를 호출합니다.

```txt
GET {BACKOFFICE_API_BASE}/api/simulators/{product_no}
```

응답 예시:

```json
{
  "enabled": true,
  "productNo": "123",
  "groups": [
    {
      "key": "topColor",
      "label": "상판 컬러",
      "values": ["메이플", "화이트", "월넛"]
    }
  ],
  "rules": [
    {
      "when": {
        "상판 컬러": "메이플",
        "프레임 컬러": "화이트"
      },
      "image": "https://example.com/simulator/123/maple-white.png"
    }
  ],
  "defaultImage": "https://example.com/simulator/123/default.png"
}
```

## 7. 문제 확인

| 증상 | 확인할 것 |
| --- | --- |
| 버튼이 안 보임 | 백오피스에서 시뮬레이터 사용이 켜져 있는지 확인 |
| 옵션을 바꿔도 이미지가 안 바뀜 | Cafe24 옵션명/옵션값과 백오피스 입력값이 같은지 확인 |
| 특정 조합만 안 보임 | 해당 조합 이미지가 등록되어 있는지 확인 |
| 이미지가 깨짐 | 업로드 이미지 URL이 정상인지 확인 |

