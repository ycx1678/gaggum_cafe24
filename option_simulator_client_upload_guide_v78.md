# 옵션 시뮬레이터 CSV 운영 가이드

> 구버전 문서입니다. 현재 운영 기준은 `option_simulator_backoffice_operation_guide_v80.md`의 백오피스 등록 방식입니다.

카페24 파일업로더 기준 / v79 구조

## 핵심 요약

이제 상품 옵션군이 달라져도 매번 HTML/CSS/JS 코드를 수정하지 않아도 됩니다.

상품번호 폴더 안에 아래 두 가지를 같이 올립니다.

```txt
1. 옵션별 PNG 이미지
2. simulator.csv
```

사이트는 해당 상품번호 폴더의 `simulator.csv`를 먼저 읽고, CSV에 적힌 옵션값과 이미지 경로를 기준으로 시뮬레이터를 구성합니다.

v79부터는 클라이언트 운영 편의를 위해 스킨 폴더보다 카페24 웹 FTP 업로드 폴더를 우선 사용합니다.

추천 위치:

```txt
/web/upload/simulator/상품번호/
```

기존 위치도 계속 지원합니다.

```txt
/nd/images/simulator/products/상품번호/
```

## 추천 운영 흐름

CSV를 직접 작성해도 되지만, 실수 방지를 위해 전달용 생성기 파일을 사용하는 것을 권장합니다.

```txt
시뮬레이터_CSV_생성기.html
```

이 파일은 서버에 설치하는 프로그램이 아닙니다. 담당자 PC에서 더블클릭으로 열고, 표를 수정한 뒤 `simulator.csv`를 다운로드하는 보조 도구입니다.

운영 순서는 아래와 같습니다.

1. 카페24 관리자에서 상품 옵션명과 옵션값을 먼저 등록합니다.
2. `시뮬레이터_CSV_생성기.html`을 엽니다.
3. 상품번호를 입력합니다.
4. 실제 상품 옵션명에 맞춰 옵션 컬럼을 추가하거나 수정합니다.
5. 옵션값별 이미지 행을 입력합니다.
6. `simulator.csv 다운로드`를 누릅니다.
7. 이미지 파일과 `simulator.csv`를 상품번호 폴더에 업로드합니다.

## 옵션 버튼 / 옵션군 추가 기준

시뮬레이터는 옵션 버튼을 새로 만들지 않습니다.

옵션 버튼, 옵션 선택 영역, 구매 버튼은 모두 기존 카페24 상품 옵션 영역을 그대로 사용합니다. 시뮬레이터는 고객이 기존 옵션 영역에서 선택한 텍스트를 읽어서 이미지만 바꿉니다.

따라서 새 옵션군이 생겼을 때의 기준은 아래와 같습니다.

| 상황 | 처리 방법 |
| --- | --- |
| 옵션 버튼/선택지를 추가해야 함 | 카페24 관리자 상품 옵션에서 추가 |
| 새 옵션군이 이미지에도 영향을 줌 | CSV 생성기에서 옵션 컬럼 추가 후 이미지 행 추가 |
| 새 옵션군이 구매에는 필요하지만 이미지에는 영향 없음 | CSV 컬럼을 만들지 않아도 됨 |
| 옵션값이 추가됨 | 카페24 옵션값 추가 후 CSV에 해당 옵션값 이미지 행 추가 |
| 기존 옵션명과 사이트 노출명이 다름 | CSV 헤더에 `옵션명|별칭` 형태로 입력 |

예를 들어 `가림판 컬러` 옵션군이 새로 생기고 이미지도 바뀌어야 한다면:

```txt
1. 카페24 상품 옵션에 가림판 컬러 추가
2. CSV 생성기에서 옵션 컬럼 추가
3. 컬럼명에 가림판 컬러 입력
4. layer를 panel로 둔 이미지 행 추가
5. panel/white.png, panel/black.png 같은 이미지 업로드
```

반대로 `배송 방식`처럼 이미지와 무관한 옵션은 CSV에 넣지 않아도 됩니다.

## 업로드 위치

상품번호가 `1034`인 경우 추천 위치:

```txt
/web/upload/simulator/1034/
```

필수 파일 예시:

```txt
/web/upload/simulator/1034/simulator.csv
/web/upload/simulator/1034/frame/basic-plus-white.png
/web/upload/simulator/1034/top/plus-edge-matte-white.png
/web/upload/simulator/1034/seat/black.png
```

기존 스킨 폴더 위치도 사용할 수 있습니다.

```txt
/nd/images/simulator/products/1034/
```

필수 파일 예시:

```txt
/nd/images/simulator/products/1034/simulator.csv
/nd/images/simulator/products/1034/frame/basic-plus-white.png
/nd/images/simulator/products/1034/top/plus-edge-matte-white.png
/nd/images/simulator/products/1034/seat/black.png
```

## CSV 기본 구조

CSV의 한 줄은 “어떤 옵션값일 때 어떤 이미지 레이어를 보여줄지”를 의미합니다.

```csv
layer,order,image,default,상판 크기|책상 사이즈,상판 형태|상판 타입,상판 컬러|책상 상판 컬러,프레임 컬러,좌판 컬러|ALO체어 컬러
frame,1,frame/basic-plus-white.png,Y,650 x 450,,,화이트,
top,2,top/plus-edge-matte-white.png,Y,650 x 450,엣지 상판,매트 화이트,,
seat,3,seat/black.png,Y,,,,,블랙
```

## 고정 컬럼 설명

| 컬럼 | 설명 |
| --- | --- |
| `layer` | 레이어 이름입니다. 예: `frame`, `top`, `seat`, `panel`, `leg` |
| `order` | 이미지가 겹쳐지는 순서입니다. 숫자가 작을수록 아래에 깔립니다. |
| `image` | 상품번호 폴더 안의 이미지 경로입니다. 예: `top/plus-edge-maple.png` |
| `default` | 기본 조합이면 `Y`를 입력합니다. 비워두면 일반 조합입니다. |

## 옵션 컬럼 설명

`default` 뒤의 컬럼들은 상품 옵션명입니다.

예:

```txt
상판 크기
상판 형태
상판 컬러
프레임 컬러
좌판 컬러
```

상품마다 옵션명이 다르면 CSV 헤더도 상품 옵션명에 맞춰 바꾸면 됩니다.

예:

```txt
의자 컬러
다리 컬러
가림판 색상
클램프 타입
```

옵션명이 관리자/사이트에서 조금씩 다르게 보일 수 있으면 `|`로 별칭을 같이 적습니다.

```txt
상판 크기|책상 사이즈|사이즈
좌판 컬러|ALO체어 컬러|의자 컬러
```

## 옵션값 입력 규칙

CSV 안의 옵션값은 사이트 옵션 버튼/선택지에 보이는 텍스트와 같게 적습니다.

```csv
top,2,top/wide-round-maple.png,,700 x 500,라운드 상판,메이플,,
seat,3,seat/deep-green.png,,,,,딥그린
```

하나의 이미지가 여러 옵션값에서 공통으로 쓰이면 `|`로 묶을 수 있습니다.

```csv
frame,1,frame/basic-plus-white.png,,600 x 400|650 x 450,,,화이트,
```

## 이미지 제작 규칙

| 항목 | 기준 |
| --- | --- |
| 파일 형식 | PNG |
| 배경 | 투명 배경 |
| 캔버스 | 같은 상품의 모든 레이어는 동일한 캔버스 크기와 위치 |
| 파일명 | 영문 소문자, 숫자, 하이픈 권장 |
| 금지 | 한글 파일명, 공백, 괄호, 특수문자 |

이미지가 커도 화면에서는 시뮬레이터 영역 밖으로 튀어나가지 않도록 자동 축소됩니다. 다만 로딩 속도를 위해 PNG 용량 최적화를 권장합니다.

## 새 상품군 추가 방법

1. 상품번호를 확인합니다.
2. `/nd/images/simulator/products/상품번호/` 폴더를 만듭니다.
3. 상품 구성에 맞춰 이미지 폴더를 만듭니다.
4. PNG 이미지를 업로드합니다.
5. 같은 폴더에 `simulator.csv`를 업로드합니다.
6. 상품 상세페이지에서 옵션을 선택해 이미지가 바뀌는지 확인합니다.

## 중요한 운영 원칙

- 같은 상품군이 아니어도 됩니다.
- 옵션명이 달라도 됩니다.
- 옵션 개수가 달라도 됩니다.
- 레이어 이름이 달라도 됩니다.
- 상품번호 폴더 안의 `simulator.csv`만 상품 구조에 맞게 작성하면 됩니다.

단, CSV 형식 자체가 깨졌거나 이미지 파일명이 실제 업로드 파일과 다르면 시뮬레이터가 비활성화되거나 일부 레이어가 표시되지 않을 수 있습니다.

## 문제 해결

| 증상 | 확인할 것 |
| --- | --- |
| 시뮬레이터 버튼이 안 보임 | `simulator.csv`가 상품번호 폴더에 있는지, 기본 조합 이미지가 있는지 확인 |
| 특정 레이어만 안 보임 | CSV의 `image` 경로와 실제 파일명이 같은지 확인 |
| 옵션 선택해도 이미지가 안 바뀜 | CSV 헤더의 옵션명이 사이트 옵션명과 맞는지 확인 |
| 이미지가 어긋남 | PNG 레이어들의 캔버스 크기와 오브젝트 위치가 같은지 확인 |
| 예전 이미지가 보임 | 브라우저 캐시 또는 카페24 캐시일 수 있으므로 강력 새로고침 후 재확인 |
