# 옵션 시뮬레이터 운영 가이드

v79 / 비개발자 운영용 상세 절차

> 구버전 문서입니다. 현재 운영 기준은 `option_simulator_backoffice_operation_guide_v80.md`의 백오피스 등록 방식입니다.

## 0. 먼저 이해해야 할 구조

옵션 시뮬레이터는 상품 옵션 버튼을 새로 만들지 않습니다.

역할은 아래처럼 나뉩니다.

| 구분 | 어디서 관리하나요? | 역할 |
| --- | --- | --- |
| 구매 옵션 버튼 | 카페24 상품 옵션 | 고객이 실제로 선택하고 주문에 반영되는 값 |
| 시뮬레이터 이미지 | 파일업로더 | 책상 상판, 프레임, 의자 등 투명 PNG 이미지 |
| 옵션값과 이미지 연결 | `simulator.csv` | 어떤 옵션값일 때 어떤 이미지를 보여줄지 정하는 표 |

즉, 옵션이 추가되면 먼저 카페24 상품 옵션을 수정하고, 그 옵션이 이미지에 영향을 주는 경우에만 `simulator.csv`도 수정합니다.

v79부터는 운영 편의를 위해 CSV와 이미지를 카페24 웹 FTP의 한 폴더에 같이 올리는 방식을 권장합니다.

추천 위치:

```txt
/web/upload/simulator/상품번호/
```

기존 스킨 폴더 위치도 계속 지원됩니다.

```txt
/nd/images/simulator/products/상품번호/
```

## 1. 작업 전에 준비할 것

### 1-1. 상품번호 확인

상품 상세 URL에서 `product_no` 값을 확인합니다.

예:

```txt
https://.../product/detail.html?product_no=1034
```

이 상품의 상품번호는 `1034`입니다.

### 1-2. 이미지에 영향을 주는 옵션만 골라내기

예를 들어 아래 옵션이 있다고 가정합니다.

```txt
책상 사이즈
상판 타입
상판 컬러
프레임 컬러
의자 컬러
배송 방식
수량
```

이 중 이미지에 영향을 주는 것은 보통 아래입니다.

```txt
책상 사이즈
상판 타입
상판 컬러
프레임 컬러
의자 컬러
```

`배송 방식`, `수량`처럼 이미지가 바뀌지 않는 옵션은 CSV에 넣지 않아도 됩니다.

### 1-3. 이미지 파일 준비

이미지는 PNG로 준비합니다.

권장 기준:

```txt
파일 형식: PNG
배경: 투명 배경
캔버스 크기: 같은 상품 안에서는 모두 동일
오브젝트 위치: 같은 상품 안에서는 모두 동일
파일명: 영문 소문자, 숫자, 하이픈 권장
```

좋은 파일명:

```txt
top/wide-edge-maple.png
frame/wide-white.png
seat/deep-green.png
```

피해야 할 파일명:

```txt
상판 메이플.png
wide edge maple.png
top(1).png
```

## 2. CSV 생성기로 `simulator.csv` 만들기

### 2-1. 생성기 열기

전달받은 파일 중 아래 파일을 더블클릭해서 엽니다.

```txt
option_simulator_csv_generator.html
```

브라우저에서 열리는 로컬 도구입니다. 서버에 설치하는 프로그램이 아닙니다.

### 2-2. 상품번호 입력

상단의 상품번호 칸에 상품번호를 입력합니다.

예:

```txt
1034
```

그러면 업로드 위치가 아래처럼 표시됩니다.

```txt
/web/upload/simulator/1034/simulator.csv
```

### 2-3. 옵션 컬럼 설정

옵션 컬럼은 “이미지에 영향을 주는 옵션명”입니다.

예:

```txt
책상 사이즈
상판 타입
상판 컬러
프레임 컬러
의자 컬러
```

사이트에서 옵션명이 다르게 보일 가능성이 있으면 `|`로 별칭을 같이 적습니다.

예:

```txt
상판 크기|책상 사이즈|사이즈
좌판 컬러|ALO체어 컬러|의자 컬러
```

이렇게 하면 사이트에서 `상판 크기`로 보이든 `책상 사이즈`로 보이든 같은 옵션군으로 인식합니다.

### 2-4. 이미지 매핑 행 입력

각 행은 “이 조건일 때 이 이미지를 보여준다”는 뜻입니다.

기본 컬럼:

| 컬럼 | 입력 예시 | 설명 |
| --- | --- | --- |
| `layer` | `top` | 레이어 이름 |
| `order` | `2` | 겹치는 순서. 숫자가 작을수록 아래 |
| `image` | `top/wide-edge-maple.png` | 상품번호 폴더 안의 이미지 경로 |
| `default` | `Y` | 기본 조합이면 Y |

예:

```csv
layer,order,image,default,책상 사이즈,상판 타입,상판 컬러,프레임 컬러,의자 컬러
frame,1,frame/wide-white.png,,700 x 500,,,화이트,
top,2,top/wide-edge-maple.png,,700 x 500,엣지 상판,메이플,,
seat,3,seat/deep-green.png,,,,,딥그린
```

### 2-5. 빈 칸의 의미

CSV에서 옵션값 칸을 비워두면 “그 옵션은 상관없음”이라는 뜻입니다.

예:

```csv
seat,3,seat/deep-green.png,,,,,딥그린
```

위 행은 책상 사이즈, 상판 타입, 상판 컬러, 프레임 컬러와 상관없이 `의자 컬러 = 딥그린`이면 `seat/deep-green.png`를 보여준다는 뜻입니다.

### 2-6. 기본 조합 지정

처음 시뮬레이터를 열었을 때 보여줄 기본 조합은 `default` 컬럼에 `Y`를 입력합니다.

예:

```csv
frame,1,frame/basic-plus-white.png,Y,650 x 450,,,화이트,
top,2,top/plus-edge-matte-white.png,Y,650 x 450,엣지 상판,매트 화이트,,
seat,3,seat/black.png,Y,,,,,블랙
```

`default=Y`는 모든 레이어에 하나씩 있는 것이 가장 안정적입니다.

### 2-7. CSV 다운로드

입력이 끝나면 생성기 상단의 버튼을 누릅니다.

```txt
simulator.csv 다운로드
```

다운로드된 파일명은 반드시 아래처럼 유지합니다.

```txt
simulator.csv
```

## 3. 카페24 파일업로더에 업로드하기

카페24 공식 도움말 기준으로 파일업로더는 `디자인 > 웹 FTP` 메뉴에서 `파일업로더 접속`을 클릭해 이용합니다.

추천 방식은 스킨 파일 폴더가 아니라 `web` 폴더 아래에 시뮬레이터 전용 폴더를 만드는 것입니다.

### 3-1. 업로드할 폴더 만들기

상품번호가 `1034`라면 아래 폴더 구조를 만듭니다.

```txt
/web/upload/simulator/1034/
```

그 안에 레이어별 폴더를 만들 수 있습니다.

예:

```txt
/web/upload/simulator/1034/frame/
/web/upload/simulator/1034/top/
/web/upload/simulator/1034/seat/
```

### 3-2. 이미지 업로드

준비한 PNG 이미지를 각 폴더에 업로드합니다.

예:

```txt
/web/upload/simulator/1034/frame/wide-white.png
/web/upload/simulator/1034/top/wide-edge-maple.png
/web/upload/simulator/1034/seat/deep-green.png
```

### 3-3. CSV 업로드

같은 상품번호 폴더에 `simulator.csv`를 업로드합니다.

예:

```txt
/web/upload/simulator/1034/simulator.csv
```

중요:

```txt
CSV는 frame/top/seat 폴더 안이 아니라 상품번호 폴더 바로 아래에 둡니다.
```

CSV의 `image` 컬럼에는 같은 상품번호 폴더 안의 상대경로만 입력하면 됩니다.

예:

```txt
top/wide-edge-maple.png
```

이미지를 다른 위치에 올린 경우에는 전체 URL이나 `/web/...` 경로를 입력해도 됩니다.

## 4. 업로드 후 확인 방법

### 4-1. 상품 상세페이지 열기

해당 상품 상세페이지를 엽니다.

### 4-2. 처음 진입 상태 확인

처음 들어갔을 때는 기존 상품 썸네일 이미지가 보이는 것이 정상입니다.

이 상태에서 시뮬레이터가 바로 크게 뜨지 않는 것이 맞습니다.

### 4-3. PC 확인

1. 상세페이지에서 아래로 스크롤합니다.
2. 우측 옵션 영역이 따라오는 상태가 됩니다.
3. 옵션 영역 왼쪽에 `옵션 시뮬레이터 보기` 버튼이 보이는지 확인합니다.
4. 버튼을 클릭합니다.
5. 시뮬레이터 이미지가 보이는지 확인합니다.
6. 옵션을 바꿔 이미지도 바뀌는지 확인합니다.

### 4-4. 모바일 확인

1. 모바일 화면에서 상세페이지를 엽니다.
2. 처음에는 기존 상품 이미지가 보이는지 확인합니다.
3. 아래로 스크롤합니다.
4. 하단에 `옵션 시뮬레이터 보기` 버튼이 보이는지 확인합니다.
5. 버튼을 누릅니다.
6. 옵션 선택 레이어와 시뮬레이터 이미지가 함께 보이는지 확인합니다.
7. 옵션을 바꿔 이미지도 바뀌는지 확인합니다.

## 5. 새 상품을 추가할 때

새 상품번호가 `1200`이라고 가정합니다.

1. 카페24 관리자에서 상품 옵션을 먼저 등록합니다.
2. 이미지에 영향을 주는 옵션군을 정리합니다.
3. 이미지 PNG를 준비합니다.
4. CSV 생성기를 엽니다.
5. 상품번호에 `1200`을 입력합니다.
6. 옵션 컬럼을 상품 옵션에 맞게 수정합니다.
7. 이미지 매핑 행을 입력합니다.
8. `simulator.csv`를 다운로드합니다.
9. 파일업로더에 아래 폴더를 만듭니다.

```txt
/web/upload/simulator/1200/
```

10. 이미지와 `simulator.csv`를 업로드합니다.
11. 상품 상세페이지에서 확인합니다.

## 6. 새 옵션값을 추가할 때

예를 들어 `의자 컬러`에 `레드`가 추가되는 경우:

1. 카페24 상품 옵션에 `레드`를 추가합니다.
2. `seat/red.png` 이미지를 준비합니다.
3. CSV 생성기를 열어 기존 `simulator.csv`를 가져옵니다.
4. 행을 추가합니다.

```csv
seat,3,seat/red.png,,,,,레드
```

5. 새 `simulator.csv`를 다운로드합니다.
6. `seat/red.png`와 `simulator.csv`를 업로드합니다.
7. 상세페이지에서 `레드` 선택 시 이미지가 바뀌는지 확인합니다.

## 7. 새 옵션군을 추가할 때

예를 들어 `가림판 컬러` 옵션군이 추가되는 경우:

### 이미지에 영향을 주는 옵션군이라면

1. 카페24 상품 옵션에 `가림판 컬러`를 추가합니다.
2. CSV 생성기에서 옵션 컬럼을 추가합니다.
3. 컬럼명을 `가림판 컬러`로 입력합니다.
4. `panel` 같은 새 레이어명을 정합니다.
5. 이미지를 준비합니다.

```txt
panel/white.png
panel/black.png
```

6. CSV 행을 추가합니다.

```csv
panel,4,panel/white.png,,,,,,화이트
panel,4,panel/black.png,,,,,,블랙
```

7. 이미지와 CSV를 업로드합니다.

### 이미지에 영향을 주지 않는 옵션군이라면

카페24 상품 옵션에만 추가하면 됩니다. CSV에는 넣지 않아도 됩니다.

## 8. 자주 나는 문제와 해결

| 증상 | 확인할 것 |
| --- | --- |
| 시뮬레이터 버튼이 안 보임 | 상품번호 폴더 바로 아래에 `simulator.csv`가 있는지 확인 |
| 버튼은 있는데 이미지가 안 보임 | CSV의 `image` 경로와 실제 파일명이 같은지 확인 |
| 특정 레이어만 안 보임 | 해당 PNG 파일이 업로드됐는지, 파일명 대소문자가 같은지 확인 |
| 옵션을 바꿔도 이미지가 안 바뀜 | CSV 옵션 컬럼명과 사이트 옵션명이 맞는지 확인 |
| 새 옵션값만 안 바뀜 | CSV 행에 옵션값을 사이트 노출 텍스트와 동일하게 적었는지 확인 |
| 이미지가 삐뚤어짐 | 모든 PNG의 캔버스 크기와 오브젝트 위치가 같은지 확인 |
| 예전 이미지가 계속 보임 | 브라우저 강력 새로고침 또는 카페24 캐시 반영 시간 확인 |

## 9. 최종 체크리스트

업로드 전:

```txt
[ ] 상품번호 확인
[ ] 이미지에 영향을 주는 옵션군 확인
[ ] PNG 파일명 영문/숫자/하이픈으로 정리
[ ] 모든 레이어 이미지 캔버스 크기 동일
[ ] CSV 파일명 simulator.csv 확인
[ ] default=Y 기본 조합 확인
```

업로드 후:

```txt
[ ] 상품번호 폴더 아래 simulator.csv 업로드 확인
[ ] 이미지 폴더와 파일 업로드 확인
[ ] PC에서 버튼 노출 확인
[ ] PC에서 옵션 변경 시 이미지 변경 확인
[ ] 모바일에서 하단 버튼 노출 확인
[ ] 모바일에서 옵션 변경 시 이미지 변경 확인
```
